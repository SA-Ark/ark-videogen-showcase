# Service configuration for ark-videogen-showcase
# Static nginx site — no Rust backend

# health_endpoint = "/health"
# This is a static site served by nginx:alpine
# Health endpoint is configured in nginx.conf
